class Solution {
    public boolean containsDuplicate(int[] nums) {
      
		int arrayLength=nums.length;
		Set<Integer> set=new HashSet<Integer>();
		for(int i=0;i<arrayLength;i++)
		{
			set.add(nums[i]);
		}
		if(set.size()<arrayLength)
		{
			return true;
		}
        return false;
    }
}
